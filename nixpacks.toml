providers = ["node"]

[variables]
NEXT_TELEMETRY_DISABLED = "1"
NODE_ENV = "production"

[phase.setup]
nixPkgs = []
nodeVersion = "22"

[phase.build]
cmds = ["npm ci", "npm run build"]

[start]
cmd = "npm start"
